services:
  gazpar2mqtt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gazpar2mqtt
    volumes:
      - ./config:/app/config
      - ./log:/app/log
    environment:
      - GRDF_USERNAME=
      - GRDF_PASSWORD=
      - GRDF_PCE_IDENTIFIER=
      - MQTT_BROKER=
      - MQTT_PORT=1883
